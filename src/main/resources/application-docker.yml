spring:
  datasource:
    url: jdbc:mysql://mysql:3306/identity_access_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    sg.edu.nus.iss.identity: INFO
